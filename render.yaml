services:
  - type: web
    name: flask-old-version
    runtime: python
    buildCommand: ""
    startCommand: gunicorn app:app
    envVars:
      - key: FLASK_ENV
        value: production
    region: oregon

